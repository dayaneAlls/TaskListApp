# 📱 TaskListApp

O TaskList é um aplicativo de lista de tarefas desenvolvido em SwiftUI, utilizando @State e @ObservableObject para gerenciamento de estado e UserDefaults para persistência local dos dados.

O projeto foi criado com o objetivo de reforçar os fundamentos do desenvolvimento iOS moderno. A ideia é praticar conceitos essenciais do SwiftUI, compreender melhor o gerenciamento de estado na prática e aplicar o padrão de arquitetura MVVM de forma simples e organizada.

Além disso, o app busca exercitar boas práticas, código limpo e a construção de uma interface declarativa, reativa e bem estruturada — servindo como base sólida para evoluir para projetos mais complexos no futuro.

---

## 🚀 Tecnologias Utilizadas

- Swift
- SwiftUI
- MVVM (Model-View-ViewModel)
- UserDefaults (persistência local)

---

## 🧱 Arquitetura

O projeto utiliza o padrão **MVVM (Model-View-ViewModel)**.

---

## 📂 Estrutura do Projeto

TaskListApp
├── Application
│   └── TaskListApp.swift
├── View
│   └── ContentView.swift
├── ViewModel
│   └── TaskViewModel.swift
└── Model
    └── Task.swift

